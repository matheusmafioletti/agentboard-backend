spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agentboard
    username: agentboard
    password: agentboard
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8082
